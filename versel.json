{
    "version": 2,
    "builds": [
      {
        "src": "app.js",
        "use": "@vercel/node"
      }
    ],
    "routes": [
      { "src": "/trips/(.*)", "dest": "/app.js" },
      { "src": "/auth/(.*)", "dest": "/app.js" }
    ]
}  